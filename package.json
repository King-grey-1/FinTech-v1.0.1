{
  "name": "fintech-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @platform/web",
    "dev:api": "npm run start:dev --workspace @platform/api",
    "build": "npm run build --workspace @platform/types && npm run build --workspace @platform/financial-engine && npm run build --workspace @platform/api && npm run build --workspace @platform/web",
    "lint": "npm run lint --workspace @platform/api && npm run lint --workspace @platform/web",
    "test": "npm run test --workspace @platform/api"
  },
  "dependencies": {
    "@types/pg": "^8.21.0",
    "pg": "^8.23.0"
  }
}
